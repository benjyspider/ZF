<?php

$form = $this->form;

$form->prepare();

// fixe l'action du formulaire à l'aide du View Helper Url (route, params)

$form->setAttribute('action', $this->url('action', array('action' => '/form' )) );

echo $this->form($form);